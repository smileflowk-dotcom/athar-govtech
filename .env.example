# Port local du démonstrateur ATHAR
ATHAR_PORT=3000

# Chemin du stockage persistant dans le conteneur
ATHAR_DATA_DIR=/data

# Extraction documentaire renforcée locale — optionnelle.
# Laisser vide pour conserver uniquement la voie rapide pdfjs-dist.
# Avec le profil Docker `enhanced-docs`, utiliser : http://docling:5001
ATHAR_DOCLING_URL=

# OCR Docling : laisser vide pour utiliser le preset du serveur.
# À fixer seulement si le corpus pilote le justifie (ex. arabic ou fr selon le moteur configuré).
ATHAR_DOCLING_OCR_LANG=
# true = OCR forcé sur toutes les pages ; false = OCR seulement lorsque nécessaire.
ATHAR_DOCLING_FORCE_OCR=false

# Assistance Gemini optionnelle — désactivée par défaut.
# Ne pas activer pour des documents sensibles sans cadre institutionnel autorisé.
ATHAR_GEMINI_ENABLED=false
ATHAR_GEMINI_MODEL=gemini-3.7-flash
GEMINI_API_KEY=
